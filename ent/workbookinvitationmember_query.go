// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"study-pal-backend/ent/predicate"
	"study-pal-backend/ent/role"
	"study-pal-backend/ent/user"
	"study-pal-backend/ent/workbook"
	"study-pal-backend/ent/workbookinvitationmember"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// WorkbookInvitationMemberQuery is the builder for querying WorkbookInvitationMember entities.
type WorkbookInvitationMemberQuery struct {
	config
	ctx          *QueryContext
	order        []workbookinvitationmember.OrderOption
	inters       []Interceptor
	predicates   []predicate.WorkbookInvitationMember
	withRole     *RoleQuery
	withUser     *UserQuery
	withWorkbook *WorkbookQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkbookInvitationMemberQuery builder.
func (wimq *WorkbookInvitationMemberQuery) Where(ps ...predicate.WorkbookInvitationMember) *WorkbookInvitationMemberQuery {
	wimq.predicates = append(wimq.predicates, ps...)
	return wimq
}

// Limit the number of records to be returned by this query.
func (wimq *WorkbookInvitationMemberQuery) Limit(limit int) *WorkbookInvitationMemberQuery {
	wimq.ctx.Limit = &limit
	return wimq
}

// Offset to start from.
func (wimq *WorkbookInvitationMemberQuery) Offset(offset int) *WorkbookInvitationMemberQuery {
	wimq.ctx.Offset = &offset
	return wimq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wimq *WorkbookInvitationMemberQuery) Unique(unique bool) *WorkbookInvitationMemberQuery {
	wimq.ctx.Unique = &unique
	return wimq
}

// Order specifies how the records should be ordered.
func (wimq *WorkbookInvitationMemberQuery) Order(o ...workbookinvitationmember.OrderOption) *WorkbookInvitationMemberQuery {
	wimq.order = append(wimq.order, o...)
	return wimq
}

// QueryRole chains the current query on the "role" edge.
func (wimq *WorkbookInvitationMemberQuery) QueryRole() *RoleQuery {
	query := (&RoleClient{config: wimq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := wimq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := wimq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workbookinvitationmember.Table, workbookinvitationmember.FieldID, selector),
			sqlgraph.To(role.Table, role.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workbookinvitationmember.RoleTable, workbookinvitationmember.RoleColumn),
		)
		fromU = sqlgraph.SetNeighbors(wimq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (wimq *WorkbookInvitationMemberQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: wimq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := wimq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := wimq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workbookinvitationmember.Table, workbookinvitationmember.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workbookinvitationmember.UserTable, workbookinvitationmember.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(wimq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWorkbook chains the current query on the "workbook" edge.
func (wimq *WorkbookInvitationMemberQuery) QueryWorkbook() *WorkbookQuery {
	query := (&WorkbookClient{config: wimq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := wimq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := wimq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workbookinvitationmember.Table, workbookinvitationmember.FieldID, selector),
			sqlgraph.To(workbook.Table, workbook.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workbookinvitationmember.WorkbookTable, workbookinvitationmember.WorkbookColumn),
		)
		fromU = sqlgraph.SetNeighbors(wimq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkbookInvitationMember entity from the query.
// Returns a *NotFoundError when no WorkbookInvitationMember was found.
func (wimq *WorkbookInvitationMemberQuery) First(ctx context.Context) (*WorkbookInvitationMember, error) {
	nodes, err := wimq.Limit(1).All(setContextOp(ctx, wimq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workbookinvitationmember.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) FirstX(ctx context.Context) *WorkbookInvitationMember {
	node, err := wimq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkbookInvitationMember ID from the query.
// Returns a *NotFoundError when no WorkbookInvitationMember ID was found.
func (wimq *WorkbookInvitationMemberQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = wimq.Limit(1).IDs(setContextOp(ctx, wimq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workbookinvitationmember.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := wimq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkbookInvitationMember entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkbookInvitationMember entity is found.
// Returns a *NotFoundError when no WorkbookInvitationMember entities are found.
func (wimq *WorkbookInvitationMemberQuery) Only(ctx context.Context) (*WorkbookInvitationMember, error) {
	nodes, err := wimq.Limit(2).All(setContextOp(ctx, wimq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workbookinvitationmember.Label}
	default:
		return nil, &NotSingularError{workbookinvitationmember.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) OnlyX(ctx context.Context) *WorkbookInvitationMember {
	node, err := wimq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkbookInvitationMember ID in the query.
// Returns a *NotSingularError when more than one WorkbookInvitationMember ID is found.
// Returns a *NotFoundError when no entities are found.
func (wimq *WorkbookInvitationMemberQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = wimq.Limit(2).IDs(setContextOp(ctx, wimq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workbookinvitationmember.Label}
	default:
		err = &NotSingularError{workbookinvitationmember.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := wimq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkbookInvitationMembers.
func (wimq *WorkbookInvitationMemberQuery) All(ctx context.Context) ([]*WorkbookInvitationMember, error) {
	ctx = setContextOp(ctx, wimq.ctx, ent.OpQueryAll)
	if err := wimq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkbookInvitationMember, *WorkbookInvitationMemberQuery]()
	return withInterceptors[[]*WorkbookInvitationMember](ctx, wimq, qr, wimq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) AllX(ctx context.Context) []*WorkbookInvitationMember {
	nodes, err := wimq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkbookInvitationMember IDs.
func (wimq *WorkbookInvitationMemberQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if wimq.ctx.Unique == nil && wimq.path != nil {
		wimq.Unique(true)
	}
	ctx = setContextOp(ctx, wimq.ctx, ent.OpQueryIDs)
	if err = wimq.Select(workbookinvitationmember.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := wimq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wimq *WorkbookInvitationMemberQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wimq.ctx, ent.OpQueryCount)
	if err := wimq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wimq, querierCount[*WorkbookInvitationMemberQuery](), wimq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) CountX(ctx context.Context) int {
	count, err := wimq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wimq *WorkbookInvitationMemberQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wimq.ctx, ent.OpQueryExist)
	switch _, err := wimq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wimq *WorkbookInvitationMemberQuery) ExistX(ctx context.Context) bool {
	exist, err := wimq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkbookInvitationMemberQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wimq *WorkbookInvitationMemberQuery) Clone() *WorkbookInvitationMemberQuery {
	if wimq == nil {
		return nil
	}
	return &WorkbookInvitationMemberQuery{
		config:       wimq.config,
		ctx:          wimq.ctx.Clone(),
		order:        append([]workbookinvitationmember.OrderOption{}, wimq.order...),
		inters:       append([]Interceptor{}, wimq.inters...),
		predicates:   append([]predicate.WorkbookInvitationMember{}, wimq.predicates...),
		withRole:     wimq.withRole.Clone(),
		withUser:     wimq.withUser.Clone(),
		withWorkbook: wimq.withWorkbook.Clone(),
		// clone intermediate query.
		sql:  wimq.sql.Clone(),
		path: wimq.path,
	}
}

// WithRole tells the query-builder to eager-load the nodes that are connected to
// the "role" edge. The optional arguments are used to configure the query builder of the edge.
func (wimq *WorkbookInvitationMemberQuery) WithRole(opts ...func(*RoleQuery)) *WorkbookInvitationMemberQuery {
	query := (&RoleClient{config: wimq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	wimq.withRole = query
	return wimq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (wimq *WorkbookInvitationMemberQuery) WithUser(opts ...func(*UserQuery)) *WorkbookInvitationMemberQuery {
	query := (&UserClient{config: wimq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	wimq.withUser = query
	return wimq
}

// WithWorkbook tells the query-builder to eager-load the nodes that are connected to
// the "workbook" edge. The optional arguments are used to configure the query builder of the edge.
func (wimq *WorkbookInvitationMemberQuery) WithWorkbook(opts ...func(*WorkbookQuery)) *WorkbookInvitationMemberQuery {
	query := (&WorkbookClient{config: wimq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	wimq.withWorkbook = query
	return wimq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkbookInvitationMember.Query().
//		GroupBy(workbookinvitationmember.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wimq *WorkbookInvitationMemberQuery) GroupBy(field string, fields ...string) *WorkbookInvitationMemberGroupBy {
	wimq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkbookInvitationMemberGroupBy{build: wimq}
	grbuild.flds = &wimq.ctx.Fields
	grbuild.label = workbookinvitationmember.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WorkbookInvitationMember.Query().
//		Select(workbookinvitationmember.FieldCreatedAt).
//		Scan(ctx, &v)
func (wimq *WorkbookInvitationMemberQuery) Select(fields ...string) *WorkbookInvitationMemberSelect {
	wimq.ctx.Fields = append(wimq.ctx.Fields, fields...)
	sbuild := &WorkbookInvitationMemberSelect{WorkbookInvitationMemberQuery: wimq}
	sbuild.label = workbookinvitationmember.Label
	sbuild.flds, sbuild.scan = &wimq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkbookInvitationMemberSelect configured with the given aggregations.
func (wimq *WorkbookInvitationMemberQuery) Aggregate(fns ...AggregateFunc) *WorkbookInvitationMemberSelect {
	return wimq.Select().Aggregate(fns...)
}

func (wimq *WorkbookInvitationMemberQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wimq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wimq); err != nil {
				return err
			}
		}
	}
	for _, f := range wimq.ctx.Fields {
		if !workbookinvitationmember.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wimq.path != nil {
		prev, err := wimq.path(ctx)
		if err != nil {
			return err
		}
		wimq.sql = prev
	}
	return nil
}

func (wimq *WorkbookInvitationMemberQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkbookInvitationMember, error) {
	var (
		nodes       = []*WorkbookInvitationMember{}
		_spec       = wimq.querySpec()
		loadedTypes = [3]bool{
			wimq.withRole != nil,
			wimq.withUser != nil,
			wimq.withWorkbook != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkbookInvitationMember).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkbookInvitationMember{config: wimq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wimq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := wimq.withRole; query != nil {
		if err := wimq.loadRole(ctx, query, nodes, nil,
			func(n *WorkbookInvitationMember, e *Role) { n.Edges.Role = e }); err != nil {
			return nil, err
		}
	}
	if query := wimq.withUser; query != nil {
		if err := wimq.loadUser(ctx, query, nodes, nil,
			func(n *WorkbookInvitationMember, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := wimq.withWorkbook; query != nil {
		if err := wimq.loadWorkbook(ctx, query, nodes, nil,
			func(n *WorkbookInvitationMember, e *Workbook) { n.Edges.Workbook = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (wimq *WorkbookInvitationMemberQuery) loadRole(ctx context.Context, query *RoleQuery, nodes []*WorkbookInvitationMember, init func(*WorkbookInvitationMember), assign func(*WorkbookInvitationMember, *Role)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkbookInvitationMember)
	for i := range nodes {
		fk := nodes[i].RoleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(role.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "role_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (wimq *WorkbookInvitationMemberQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*WorkbookInvitationMember, init func(*WorkbookInvitationMember), assign func(*WorkbookInvitationMember, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkbookInvitationMember)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (wimq *WorkbookInvitationMemberQuery) loadWorkbook(ctx context.Context, query *WorkbookQuery, nodes []*WorkbookInvitationMember, init func(*WorkbookInvitationMember), assign func(*WorkbookInvitationMember, *Workbook)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkbookInvitationMember)
	for i := range nodes {
		fk := nodes[i].WorkbookID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workbook.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "workbook_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (wimq *WorkbookInvitationMemberQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wimq.querySpec()
	_spec.Node.Columns = wimq.ctx.Fields
	if len(wimq.ctx.Fields) > 0 {
		_spec.Unique = wimq.ctx.Unique != nil && *wimq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wimq.driver, _spec)
}

func (wimq *WorkbookInvitationMemberQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workbookinvitationmember.Table, workbookinvitationmember.Columns, sqlgraph.NewFieldSpec(workbookinvitationmember.FieldID, field.TypeUUID))
	_spec.From = wimq.sql
	if unique := wimq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wimq.path != nil {
		_spec.Unique = true
	}
	if fields := wimq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workbookinvitationmember.FieldID)
		for i := range fields {
			if fields[i] != workbookinvitationmember.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if wimq.withRole != nil {
			_spec.Node.AddColumnOnce(workbookinvitationmember.FieldRoleID)
		}
		if wimq.withUser != nil {
			_spec.Node.AddColumnOnce(workbookinvitationmember.FieldUserID)
		}
		if wimq.withWorkbook != nil {
			_spec.Node.AddColumnOnce(workbookinvitationmember.FieldWorkbookID)
		}
	}
	if ps := wimq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wimq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wimq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wimq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wimq *WorkbookInvitationMemberQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wimq.driver.Dialect())
	t1 := builder.Table(workbookinvitationmember.Table)
	columns := wimq.ctx.Fields
	if len(columns) == 0 {
		columns = workbookinvitationmember.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wimq.sql != nil {
		selector = wimq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wimq.ctx.Unique != nil && *wimq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wimq.predicates {
		p(selector)
	}
	for _, p := range wimq.order {
		p(selector)
	}
	if offset := wimq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wimq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WorkbookInvitationMemberGroupBy is the group-by builder for WorkbookInvitationMember entities.
type WorkbookInvitationMemberGroupBy struct {
	selector
	build *WorkbookInvitationMemberQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wimgb *WorkbookInvitationMemberGroupBy) Aggregate(fns ...AggregateFunc) *WorkbookInvitationMemberGroupBy {
	wimgb.fns = append(wimgb.fns, fns...)
	return wimgb
}

// Scan applies the selector query and scans the result into the given value.
func (wimgb *WorkbookInvitationMemberGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wimgb.build.ctx, ent.OpQueryGroupBy)
	if err := wimgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkbookInvitationMemberQuery, *WorkbookInvitationMemberGroupBy](ctx, wimgb.build, wimgb, wimgb.build.inters, v)
}

func (wimgb *WorkbookInvitationMemberGroupBy) sqlScan(ctx context.Context, root *WorkbookInvitationMemberQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wimgb.fns))
	for _, fn := range wimgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wimgb.flds)+len(wimgb.fns))
		for _, f := range *wimgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wimgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wimgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkbookInvitationMemberSelect is the builder for selecting fields of WorkbookInvitationMember entities.
type WorkbookInvitationMemberSelect struct {
	*WorkbookInvitationMemberQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wims *WorkbookInvitationMemberSelect) Aggregate(fns ...AggregateFunc) *WorkbookInvitationMemberSelect {
	wims.fns = append(wims.fns, fns...)
	return wims
}

// Scan applies the selector query and scans the result into the given value.
func (wims *WorkbookInvitationMemberSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wims.ctx, ent.OpQuerySelect)
	if err := wims.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkbookInvitationMemberQuery, *WorkbookInvitationMemberSelect](ctx, wims.WorkbookInvitationMemberQuery, wims, wims.inters, v)
}

func (wims *WorkbookInvitationMemberSelect) sqlScan(ctx context.Context, root *WorkbookInvitationMemberQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wims.fns))
	for _, fn := range wims.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wims.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wims.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
