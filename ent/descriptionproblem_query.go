// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"study-pal-backend/ent/descriptionproblem"
	"study-pal-backend/ent/predicate"
	"study-pal-backend/ent/workbook"
	"study-pal-backend/ent/workbookcategory"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// DescriptionProblemQuery is the builder for querying DescriptionProblem entities.
type DescriptionProblemQuery struct {
	config
	ctx                  *QueryContext
	order                []descriptionproblem.OrderOption
	inters               []Interceptor
	predicates           []predicate.DescriptionProblem
	withWorkbook         *WorkbookQuery
	withWorkbookCategory *WorkbookCategoryQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DescriptionProblemQuery builder.
func (dpq *DescriptionProblemQuery) Where(ps ...predicate.DescriptionProblem) *DescriptionProblemQuery {
	dpq.predicates = append(dpq.predicates, ps...)
	return dpq
}

// Limit the number of records to be returned by this query.
func (dpq *DescriptionProblemQuery) Limit(limit int) *DescriptionProblemQuery {
	dpq.ctx.Limit = &limit
	return dpq
}

// Offset to start from.
func (dpq *DescriptionProblemQuery) Offset(offset int) *DescriptionProblemQuery {
	dpq.ctx.Offset = &offset
	return dpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dpq *DescriptionProblemQuery) Unique(unique bool) *DescriptionProblemQuery {
	dpq.ctx.Unique = &unique
	return dpq
}

// Order specifies how the records should be ordered.
func (dpq *DescriptionProblemQuery) Order(o ...descriptionproblem.OrderOption) *DescriptionProblemQuery {
	dpq.order = append(dpq.order, o...)
	return dpq
}

// QueryWorkbook chains the current query on the "workbook" edge.
func (dpq *DescriptionProblemQuery) QueryWorkbook() *WorkbookQuery {
	query := (&WorkbookClient{config: dpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(descriptionproblem.Table, descriptionproblem.FieldID, selector),
			sqlgraph.To(workbook.Table, workbook.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, descriptionproblem.WorkbookTable, descriptionproblem.WorkbookColumn),
		)
		fromU = sqlgraph.SetNeighbors(dpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWorkbookCategory chains the current query on the "workbook_category" edge.
func (dpq *DescriptionProblemQuery) QueryWorkbookCategory() *WorkbookCategoryQuery {
	query := (&WorkbookCategoryClient{config: dpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(descriptionproblem.Table, descriptionproblem.FieldID, selector),
			sqlgraph.To(workbookcategory.Table, workbookcategory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, descriptionproblem.WorkbookCategoryTable, descriptionproblem.WorkbookCategoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(dpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DescriptionProblem entity from the query.
// Returns a *NotFoundError when no DescriptionProblem was found.
func (dpq *DescriptionProblemQuery) First(ctx context.Context) (*DescriptionProblem, error) {
	nodes, err := dpq.Limit(1).All(setContextOp(ctx, dpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{descriptionproblem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) FirstX(ctx context.Context) *DescriptionProblem {
	node, err := dpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DescriptionProblem ID from the query.
// Returns a *NotFoundError when no DescriptionProblem ID was found.
func (dpq *DescriptionProblemQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dpq.Limit(1).IDs(setContextOp(ctx, dpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{descriptionproblem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := dpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DescriptionProblem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DescriptionProblem entity is found.
// Returns a *NotFoundError when no DescriptionProblem entities are found.
func (dpq *DescriptionProblemQuery) Only(ctx context.Context) (*DescriptionProblem, error) {
	nodes, err := dpq.Limit(2).All(setContextOp(ctx, dpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{descriptionproblem.Label}
	default:
		return nil, &NotSingularError{descriptionproblem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) OnlyX(ctx context.Context) *DescriptionProblem {
	node, err := dpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DescriptionProblem ID in the query.
// Returns a *NotSingularError when more than one DescriptionProblem ID is found.
// Returns a *NotFoundError when no entities are found.
func (dpq *DescriptionProblemQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = dpq.Limit(2).IDs(setContextOp(ctx, dpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{descriptionproblem.Label}
	default:
		err = &NotSingularError{descriptionproblem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := dpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DescriptionProblems.
func (dpq *DescriptionProblemQuery) All(ctx context.Context) ([]*DescriptionProblem, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryAll)
	if err := dpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DescriptionProblem, *DescriptionProblemQuery]()
	return withInterceptors[[]*DescriptionProblem](ctx, dpq, qr, dpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) AllX(ctx context.Context) []*DescriptionProblem {
	nodes, err := dpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DescriptionProblem IDs.
func (dpq *DescriptionProblemQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if dpq.ctx.Unique == nil && dpq.path != nil {
		dpq.Unique(true)
	}
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryIDs)
	if err = dpq.Select(descriptionproblem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := dpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dpq *DescriptionProblemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryCount)
	if err := dpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dpq, querierCount[*DescriptionProblemQuery](), dpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) CountX(ctx context.Context) int {
	count, err := dpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dpq *DescriptionProblemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dpq.ctx, ent.OpQueryExist)
	switch _, err := dpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dpq *DescriptionProblemQuery) ExistX(ctx context.Context) bool {
	exist, err := dpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DescriptionProblemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dpq *DescriptionProblemQuery) Clone() *DescriptionProblemQuery {
	if dpq == nil {
		return nil
	}
	return &DescriptionProblemQuery{
		config:               dpq.config,
		ctx:                  dpq.ctx.Clone(),
		order:                append([]descriptionproblem.OrderOption{}, dpq.order...),
		inters:               append([]Interceptor{}, dpq.inters...),
		predicates:           append([]predicate.DescriptionProblem{}, dpq.predicates...),
		withWorkbook:         dpq.withWorkbook.Clone(),
		withWorkbookCategory: dpq.withWorkbookCategory.Clone(),
		// clone intermediate query.
		sql:  dpq.sql.Clone(),
		path: dpq.path,
	}
}

// WithWorkbook tells the query-builder to eager-load the nodes that are connected to
// the "workbook" edge. The optional arguments are used to configure the query builder of the edge.
func (dpq *DescriptionProblemQuery) WithWorkbook(opts ...func(*WorkbookQuery)) *DescriptionProblemQuery {
	query := (&WorkbookClient{config: dpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dpq.withWorkbook = query
	return dpq
}

// WithWorkbookCategory tells the query-builder to eager-load the nodes that are connected to
// the "workbook_category" edge. The optional arguments are used to configure the query builder of the edge.
func (dpq *DescriptionProblemQuery) WithWorkbookCategory(opts ...func(*WorkbookCategoryQuery)) *DescriptionProblemQuery {
	query := (&WorkbookCategoryClient{config: dpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dpq.withWorkbookCategory = query
	return dpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DescriptionProblem.Query().
//		GroupBy(descriptionproblem.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dpq *DescriptionProblemQuery) GroupBy(field string, fields ...string) *DescriptionProblemGroupBy {
	dpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DescriptionProblemGroupBy{build: dpq}
	grbuild.flds = &dpq.ctx.Fields
	grbuild.label = descriptionproblem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.DescriptionProblem.Query().
//		Select(descriptionproblem.FieldCreatedAt).
//		Scan(ctx, &v)
func (dpq *DescriptionProblemQuery) Select(fields ...string) *DescriptionProblemSelect {
	dpq.ctx.Fields = append(dpq.ctx.Fields, fields...)
	sbuild := &DescriptionProblemSelect{DescriptionProblemQuery: dpq}
	sbuild.label = descriptionproblem.Label
	sbuild.flds, sbuild.scan = &dpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DescriptionProblemSelect configured with the given aggregations.
func (dpq *DescriptionProblemQuery) Aggregate(fns ...AggregateFunc) *DescriptionProblemSelect {
	return dpq.Select().Aggregate(fns...)
}

func (dpq *DescriptionProblemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dpq); err != nil {
				return err
			}
		}
	}
	for _, f := range dpq.ctx.Fields {
		if !descriptionproblem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dpq.path != nil {
		prev, err := dpq.path(ctx)
		if err != nil {
			return err
		}
		dpq.sql = prev
	}
	return nil
}

func (dpq *DescriptionProblemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DescriptionProblem, error) {
	var (
		nodes       = []*DescriptionProblem{}
		_spec       = dpq.querySpec()
		loadedTypes = [2]bool{
			dpq.withWorkbook != nil,
			dpq.withWorkbookCategory != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DescriptionProblem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DescriptionProblem{config: dpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dpq.withWorkbook; query != nil {
		if err := dpq.loadWorkbook(ctx, query, nodes, nil,
			func(n *DescriptionProblem, e *Workbook) { n.Edges.Workbook = e }); err != nil {
			return nil, err
		}
	}
	if query := dpq.withWorkbookCategory; query != nil {
		if err := dpq.loadWorkbookCategory(ctx, query, nodes, nil,
			func(n *DescriptionProblem, e *WorkbookCategory) { n.Edges.WorkbookCategory = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dpq *DescriptionProblemQuery) loadWorkbook(ctx context.Context, query *WorkbookQuery, nodes []*DescriptionProblem, init func(*DescriptionProblem), assign func(*DescriptionProblem, *Workbook)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DescriptionProblem)
	for i := range nodes {
		fk := nodes[i].WorkbookID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workbook.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "workbook_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dpq *DescriptionProblemQuery) loadWorkbookCategory(ctx context.Context, query *WorkbookCategoryQuery, nodes []*DescriptionProblem, init func(*DescriptionProblem), assign func(*DescriptionProblem, *WorkbookCategory)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*DescriptionProblem)
	for i := range nodes {
		if nodes[i].WorkbookCategoryID == nil {
			continue
		}
		fk := *nodes[i].WorkbookCategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(workbookcategory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "workbook_category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dpq *DescriptionProblemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dpq.querySpec()
	_spec.Node.Columns = dpq.ctx.Fields
	if len(dpq.ctx.Fields) > 0 {
		_spec.Unique = dpq.ctx.Unique != nil && *dpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dpq.driver, _spec)
}

func (dpq *DescriptionProblemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(descriptionproblem.Table, descriptionproblem.Columns, sqlgraph.NewFieldSpec(descriptionproblem.FieldID, field.TypeUUID))
	_spec.From = dpq.sql
	if unique := dpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dpq.path != nil {
		_spec.Unique = true
	}
	if fields := dpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, descriptionproblem.FieldID)
		for i := range fields {
			if fields[i] != descriptionproblem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dpq.withWorkbook != nil {
			_spec.Node.AddColumnOnce(descriptionproblem.FieldWorkbookID)
		}
		if dpq.withWorkbookCategory != nil {
			_spec.Node.AddColumnOnce(descriptionproblem.FieldWorkbookCategoryID)
		}
	}
	if ps := dpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dpq *DescriptionProblemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dpq.driver.Dialect())
	t1 := builder.Table(descriptionproblem.Table)
	columns := dpq.ctx.Fields
	if len(columns) == 0 {
		columns = descriptionproblem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dpq.sql != nil {
		selector = dpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dpq.ctx.Unique != nil && *dpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dpq.predicates {
		p(selector)
	}
	for _, p := range dpq.order {
		p(selector)
	}
	if offset := dpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DescriptionProblemGroupBy is the group-by builder for DescriptionProblem entities.
type DescriptionProblemGroupBy struct {
	selector
	build *DescriptionProblemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dpgb *DescriptionProblemGroupBy) Aggregate(fns ...AggregateFunc) *DescriptionProblemGroupBy {
	dpgb.fns = append(dpgb.fns, fns...)
	return dpgb
}

// Scan applies the selector query and scans the result into the given value.
func (dpgb *DescriptionProblemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dpgb.build.ctx, ent.OpQueryGroupBy)
	if err := dpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DescriptionProblemQuery, *DescriptionProblemGroupBy](ctx, dpgb.build, dpgb, dpgb.build.inters, v)
}

func (dpgb *DescriptionProblemGroupBy) sqlScan(ctx context.Context, root *DescriptionProblemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dpgb.fns))
	for _, fn := range dpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dpgb.flds)+len(dpgb.fns))
		for _, f := range *dpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DescriptionProblemSelect is the builder for selecting fields of DescriptionProblem entities.
type DescriptionProblemSelect struct {
	*DescriptionProblemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dps *DescriptionProblemSelect) Aggregate(fns ...AggregateFunc) *DescriptionProblemSelect {
	dps.fns = append(dps.fns, fns...)
	return dps
}

// Scan applies the selector query and scans the result into the given value.
func (dps *DescriptionProblemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dps.ctx, ent.OpQuerySelect)
	if err := dps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DescriptionProblemQuery, *DescriptionProblemSelect](ctx, dps.DescriptionProblemQuery, dps, dps.inters, v)
}

func (dps *DescriptionProblemSelect) sqlScan(ctx context.Context, root *DescriptionProblemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dps.fns))
	for _, fn := range dps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
