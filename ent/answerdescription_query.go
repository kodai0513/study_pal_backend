// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"study-pal-backend/ent/answerdescription"
	"study-pal-backend/ent/predicate"
	"study-pal-backend/ent/problem"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AnswerDescriptionQuery is the builder for querying AnswerDescription entities.
type AnswerDescriptionQuery struct {
	config
	ctx         *QueryContext
	order       []answerdescription.OrderOption
	inters      []Interceptor
	predicates  []predicate.AnswerDescription
	withProblem *ProblemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AnswerDescriptionQuery builder.
func (adq *AnswerDescriptionQuery) Where(ps ...predicate.AnswerDescription) *AnswerDescriptionQuery {
	adq.predicates = append(adq.predicates, ps...)
	return adq
}

// Limit the number of records to be returned by this query.
func (adq *AnswerDescriptionQuery) Limit(limit int) *AnswerDescriptionQuery {
	adq.ctx.Limit = &limit
	return adq
}

// Offset to start from.
func (adq *AnswerDescriptionQuery) Offset(offset int) *AnswerDescriptionQuery {
	adq.ctx.Offset = &offset
	return adq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (adq *AnswerDescriptionQuery) Unique(unique bool) *AnswerDescriptionQuery {
	adq.ctx.Unique = &unique
	return adq
}

// Order specifies how the records should be ordered.
func (adq *AnswerDescriptionQuery) Order(o ...answerdescription.OrderOption) *AnswerDescriptionQuery {
	adq.order = append(adq.order, o...)
	return adq
}

// QueryProblem chains the current query on the "problem" edge.
func (adq *AnswerDescriptionQuery) QueryProblem() *ProblemQuery {
	query := (&ProblemClient{config: adq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := adq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := adq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(answerdescription.Table, answerdescription.FieldID, selector),
			sqlgraph.To(problem.Table, problem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, answerdescription.ProblemTable, answerdescription.ProblemColumn),
		)
		fromU = sqlgraph.SetNeighbors(adq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AnswerDescription entity from the query.
// Returns a *NotFoundError when no AnswerDescription was found.
func (adq *AnswerDescriptionQuery) First(ctx context.Context) (*AnswerDescription, error) {
	nodes, err := adq.Limit(1).All(setContextOp(ctx, adq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{answerdescription.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) FirstX(ctx context.Context) *AnswerDescription {
	node, err := adq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AnswerDescription ID from the query.
// Returns a *NotFoundError when no AnswerDescription ID was found.
func (adq *AnswerDescriptionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = adq.Limit(1).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{answerdescription.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) FirstIDX(ctx context.Context) int {
	id, err := adq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AnswerDescription entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AnswerDescription entity is found.
// Returns a *NotFoundError when no AnswerDescription entities are found.
func (adq *AnswerDescriptionQuery) Only(ctx context.Context) (*AnswerDescription, error) {
	nodes, err := adq.Limit(2).All(setContextOp(ctx, adq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{answerdescription.Label}
	default:
		return nil, &NotSingularError{answerdescription.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) OnlyX(ctx context.Context) *AnswerDescription {
	node, err := adq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AnswerDescription ID in the query.
// Returns a *NotSingularError when more than one AnswerDescription ID is found.
// Returns a *NotFoundError when no entities are found.
func (adq *AnswerDescriptionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = adq.Limit(2).IDs(setContextOp(ctx, adq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{answerdescription.Label}
	default:
		err = &NotSingularError{answerdescription.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) OnlyIDX(ctx context.Context) int {
	id, err := adq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AnswerDescriptions.
func (adq *AnswerDescriptionQuery) All(ctx context.Context) ([]*AnswerDescription, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryAll)
	if err := adq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AnswerDescription, *AnswerDescriptionQuery]()
	return withInterceptors[[]*AnswerDescription](ctx, adq, qr, adq.inters)
}

// AllX is like All, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) AllX(ctx context.Context) []*AnswerDescription {
	nodes, err := adq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AnswerDescription IDs.
func (adq *AnswerDescriptionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if adq.ctx.Unique == nil && adq.path != nil {
		adq.Unique(true)
	}
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryIDs)
	if err = adq.Select(answerdescription.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) IDsX(ctx context.Context) []int {
	ids, err := adq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (adq *AnswerDescriptionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryCount)
	if err := adq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, adq, querierCount[*AnswerDescriptionQuery](), adq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) CountX(ctx context.Context) int {
	count, err := adq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (adq *AnswerDescriptionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, adq.ctx, ent.OpQueryExist)
	switch _, err := adq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (adq *AnswerDescriptionQuery) ExistX(ctx context.Context) bool {
	exist, err := adq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AnswerDescriptionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (adq *AnswerDescriptionQuery) Clone() *AnswerDescriptionQuery {
	if adq == nil {
		return nil
	}
	return &AnswerDescriptionQuery{
		config:      adq.config,
		ctx:         adq.ctx.Clone(),
		order:       append([]answerdescription.OrderOption{}, adq.order...),
		inters:      append([]Interceptor{}, adq.inters...),
		predicates:  append([]predicate.AnswerDescription{}, adq.predicates...),
		withProblem: adq.withProblem.Clone(),
		// clone intermediate query.
		sql:  adq.sql.Clone(),
		path: adq.path,
	}
}

// WithProblem tells the query-builder to eager-load the nodes that are connected to
// the "problem" edge. The optional arguments are used to configure the query builder of the edge.
func (adq *AnswerDescriptionQuery) WithProblem(opts ...func(*ProblemQuery)) *AnswerDescriptionQuery {
	query := (&ProblemClient{config: adq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	adq.withProblem = query
	return adq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AnswerDescription.Query().
//		GroupBy(answerdescription.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (adq *AnswerDescriptionQuery) GroupBy(field string, fields ...string) *AnswerDescriptionGroupBy {
	adq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AnswerDescriptionGroupBy{build: adq}
	grbuild.flds = &adq.ctx.Fields
	grbuild.label = answerdescription.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AnswerDescription.Query().
//		Select(answerdescription.FieldCreatedAt).
//		Scan(ctx, &v)
func (adq *AnswerDescriptionQuery) Select(fields ...string) *AnswerDescriptionSelect {
	adq.ctx.Fields = append(adq.ctx.Fields, fields...)
	sbuild := &AnswerDescriptionSelect{AnswerDescriptionQuery: adq}
	sbuild.label = answerdescription.Label
	sbuild.flds, sbuild.scan = &adq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AnswerDescriptionSelect configured with the given aggregations.
func (adq *AnswerDescriptionQuery) Aggregate(fns ...AggregateFunc) *AnswerDescriptionSelect {
	return adq.Select().Aggregate(fns...)
}

func (adq *AnswerDescriptionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range adq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, adq); err != nil {
				return err
			}
		}
	}
	for _, f := range adq.ctx.Fields {
		if !answerdescription.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if adq.path != nil {
		prev, err := adq.path(ctx)
		if err != nil {
			return err
		}
		adq.sql = prev
	}
	return nil
}

func (adq *AnswerDescriptionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AnswerDescription, error) {
	var (
		nodes       = []*AnswerDescription{}
		_spec       = adq.querySpec()
		loadedTypes = [1]bool{
			adq.withProblem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AnswerDescription).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AnswerDescription{config: adq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, adq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := adq.withProblem; query != nil {
		if err := adq.loadProblem(ctx, query, nodes, nil,
			func(n *AnswerDescription, e *Problem) { n.Edges.Problem = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (adq *AnswerDescriptionQuery) loadProblem(ctx context.Context, query *ProblemQuery, nodes []*AnswerDescription, init func(*AnswerDescription), assign func(*AnswerDescription, *Problem)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AnswerDescription)
	for i := range nodes {
		fk := nodes[i].ProblemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(problem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "problem_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (adq *AnswerDescriptionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := adq.querySpec()
	_spec.Node.Columns = adq.ctx.Fields
	if len(adq.ctx.Fields) > 0 {
		_spec.Unique = adq.ctx.Unique != nil && *adq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, adq.driver, _spec)
}

func (adq *AnswerDescriptionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(answerdescription.Table, answerdescription.Columns, sqlgraph.NewFieldSpec(answerdescription.FieldID, field.TypeInt))
	_spec.From = adq.sql
	if unique := adq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if adq.path != nil {
		_spec.Unique = true
	}
	if fields := adq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, answerdescription.FieldID)
		for i := range fields {
			if fields[i] != answerdescription.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if adq.withProblem != nil {
			_spec.Node.AddColumnOnce(answerdescription.FieldProblemID)
		}
	}
	if ps := adq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := adq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := adq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := adq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (adq *AnswerDescriptionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(adq.driver.Dialect())
	t1 := builder.Table(answerdescription.Table)
	columns := adq.ctx.Fields
	if len(columns) == 0 {
		columns = answerdescription.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if adq.sql != nil {
		selector = adq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if adq.ctx.Unique != nil && *adq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range adq.predicates {
		p(selector)
	}
	for _, p := range adq.order {
		p(selector)
	}
	if offset := adq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := adq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AnswerDescriptionGroupBy is the group-by builder for AnswerDescription entities.
type AnswerDescriptionGroupBy struct {
	selector
	build *AnswerDescriptionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (adgb *AnswerDescriptionGroupBy) Aggregate(fns ...AggregateFunc) *AnswerDescriptionGroupBy {
	adgb.fns = append(adgb.fns, fns...)
	return adgb
}

// Scan applies the selector query and scans the result into the given value.
func (adgb *AnswerDescriptionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, adgb.build.ctx, ent.OpQueryGroupBy)
	if err := adgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnswerDescriptionQuery, *AnswerDescriptionGroupBy](ctx, adgb.build, adgb, adgb.build.inters, v)
}

func (adgb *AnswerDescriptionGroupBy) sqlScan(ctx context.Context, root *AnswerDescriptionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(adgb.fns))
	for _, fn := range adgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*adgb.flds)+len(adgb.fns))
		for _, f := range *adgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*adgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := adgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AnswerDescriptionSelect is the builder for selecting fields of AnswerDescription entities.
type AnswerDescriptionSelect struct {
	*AnswerDescriptionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ads *AnswerDescriptionSelect) Aggregate(fns ...AggregateFunc) *AnswerDescriptionSelect {
	ads.fns = append(ads.fns, fns...)
	return ads
}

// Scan applies the selector query and scans the result into the given value.
func (ads *AnswerDescriptionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ads.ctx, ent.OpQuerySelect)
	if err := ads.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnswerDescriptionQuery, *AnswerDescriptionSelect](ctx, ads.AnswerDescriptionQuery, ads, ads.inters, v)
}

func (ads *AnswerDescriptionSelect) sqlScan(ctx context.Context, root *AnswerDescriptionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ads.fns))
	for _, fn := range ads.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ads.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ads.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
